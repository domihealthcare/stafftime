# Web frontend — not started

Placeholder for the React + Vite + Tailwind app (Phase 1: clock-in screen,
timesheet view, manager shift scheduler).

The workspace exists so the frontend can be added without restructuring the
repository. See `docs/architecture.md`.
