{
  "name": "stafftime",
  "version": "0.1.0",
  "private": true,
  "description": "Domi Time & Scheduling — staff clock-in, scheduling and timesheets",
  "workspaces": [
    "apps/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,magenta \"npm run dev:api\" \"npm run dev:web\"",
    "build": "npm run build --workspace @stafftime/api && npm run build --workspace @stafftime/web",
    "test": "npm run test --workspace @stafftime/api",
    "test:browser": "bash tests/browser/run-all.sh",
    "lint": "npm run lint --workspace @stafftime/api && npm run lint --workspace @stafftime/web",
    "format": "prettier --write \"apps/**/*.{ts,tsx,js,json,md}\"",
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "db:migrate": "npm run prisma:migrate --workspace @stafftime/api",
    "db:seed": "npm run prisma:seed --workspace @stafftime/api",
    "db:studio": "npm run prisma:studio --workspace @stafftime/api",
    "dev:api": "npm run start:dev --workspace @stafftime/api",
    "dev:web": "npm run dev --workspace @stafftime/web",
    "typecheck": "npm run typecheck --workspace @stafftime/web",
    "preview": "npm run build --workspace @stafftime/web && npm run preview --workspace @stafftime/web",
    "vercel-build": "npm run prisma:generate --workspace @stafftime/api && npm run prisma:deploy --workspace @stafftime/api && npm run build --workspace @stafftime/web"
  },
  "devDependencies": {
    "@vercel/node": "^5.10.2",
    "concurrently": "^9.0.1",
    "prettier": "^3.3.3"
  }
}
